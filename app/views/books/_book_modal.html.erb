      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h3 class="modal-title"><%= book.title %></h3>
        <h4 class="modal-title"><%= book.authors.collect{|a| a.name}.join(" / ") %></h4>
      </div>
      <div class="modal-body">
        <img class="pull-left img-responsive cover" src="<%= book.cover.url(:medium) %>" alt="'<%= book.title %>' Cover">
        <span><%= book.description.gsub("\n","<br>").html_safe %></span>
        <span class="clearfix"></span>
      </div>
      <div class="modal-footer">
        <%= link_to "Read Now!", "#", class: "btn btn-primary" if user_signed_in? %>
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
      </div>
